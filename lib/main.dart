import 'package:flutter/material.dart';


void main() {
  runApp(SGM());
}

class SGM extends StatelessWidget {
  const SGM({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext cosntext) {
    return MaterialApp(home: Container());
  }
}
